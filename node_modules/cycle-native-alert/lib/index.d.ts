import { Stream } from 'xstream';
export declare type ButtonConfig = {
    text: string;
    id: ButtonId;
    style?: 'default' | 'cancel' | 'destructive';
};
export declare type Command = {
    title: string;
    message?: string;
    buttons?: Array<ButtonConfig>;
    options?: {
        cancelable: boolean;
    };
    type?: any;
};
export declare type ButtonId = string;
export declare function alertDriver(command$: Stream<Command>): Stream<ButtonId>;
