import { Stream } from 'xstream';
export declare type SharedContent = {
    id?: number;
    message: string;
    title: string;
    dialogTitle?: string;
};
export declare function shareDriver(sink: Stream<SharedContent>): Stream<number>;
