import { Stream } from 'xstream';
export declare type DisappearEvent = {
    componentId: string;
    componentName: string;
};
export declare class NavSource {
    _topBar: Stream<string>;
    _back: Stream<null>;
    _didAppear: Stream<null>;
    _didDisappear: Stream<null>;
    _globalDidDisappear: Stream<DisappearEvent>;
    constructor();
    topBarButtonPress(buttonId?: string): Stream<string>;
    backPress(): Stream<null>;
    didAppear(): Stream<null>;
    didDisappear(): Stream<null>;
    globalDidDisappear(componentName?: string): Stream<DisappearEvent>;
}
