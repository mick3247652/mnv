import { Stream } from 'xstream';
export declare type Handlers = {
    [selector: string]: {
        [evType: string]: Stream<any>;
    };
};
export declare type Listeners = {
    [selector: string]: () => void;
};
export declare class Scope {
    handlers: Handlers;
    listeners: Listeners;
    constructor();
    getSelectorHandlers(selector: string | symbol): {
        [evType: string]: Stream<any>;
    };
    getHandler(selector: string | symbol, evType: string): Stream<any>;
    subscribe(selector: string | symbol, listener: () => void): () => void;
}
