"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = require("react");
var incorporate_1 = require("./incorporate");
function createElementSpreading(type, props, children) {
    if (typeof children === 'string') {
        return react_1.createElement(type, props, children);
    }
    else {
        return react_1.createElement.apply(void 0, [type, props].concat(children));
    }
}
function hyperscriptProps(type, props) {
    if (!props.sel) {
        return react_1.createElement(type, props);
    }
    else {
        return react_1.createElement(incorporate_1.incorporate(type), props);
    }
}
function hyperscriptChildren(type, children) {
    return createElementSpreading(type, null, children);
}
function hyperscriptPropsChildren(type, props, children) {
    if (!props.sel) {
        return createElementSpreading(type, props, children);
    }
    else {
        return createElementSpreading(incorporate_1.incorporate(type), props, children);
    }
}
function h(type, a, b) {
    if (a === undefined && b === undefined) {
        return react_1.createElement(type, null);
    }
    if (b === undefined && (typeof a === 'string' || Array.isArray(a))) {
        return hyperscriptChildren(type, a);
    }
    if (b === undefined && typeof a === 'object' && !Array.isArray(a)) {
        return hyperscriptProps(type, a);
    }
    if (a !== undefined &&
        typeof a !== 'string' &&
        !Array.isArray(a) &&
        b !== undefined) {
        return hyperscriptPropsChildren(type, a, b);
    }
    else {
        throw new Error('Unexpected usage of h() function');
    }
}
exports.h = h;
//# sourceMappingURL=h.js.map