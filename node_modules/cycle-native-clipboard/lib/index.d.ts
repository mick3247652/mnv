import { Stream } from 'xstream';
export declare class ClipboardSource {
    constructor();
    getString(): Stream<string>;
}
export declare function makeClipboardDriver(): (sink: Stream<string>) => ClipboardSource;
