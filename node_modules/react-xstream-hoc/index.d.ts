import { Stream } from 'xstream';
import { ComponentClass as CClass } from 'react';
export declare type XstreamProps<P, K extends keyof P> = {
    [Key in K]: P[Key] | Stream<P[Key]>;
};
export declare function withXstreamProps<P, K extends keyof P, M extends K>(Comp: CClass<P>, ...names: Array<M>): CClass<XstreamProps<P, K>>;
